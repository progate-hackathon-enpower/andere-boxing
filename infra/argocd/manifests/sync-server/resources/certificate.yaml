apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sync-server-tls
  namespace: sync-server
spec:
  secretName: sync-server-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.qwet.app"
    - "qwet.app"
